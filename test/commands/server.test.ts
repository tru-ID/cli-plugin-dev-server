import {expect, test} from '@oclif/test'

describe('server', () => {
  // test
  // .stdout()
  // .command(['server', '--help'])
  // .it('runs server -h', ctx => {
  //   expect(ctx.stdout).to.contain('tru server')
  // })
})
